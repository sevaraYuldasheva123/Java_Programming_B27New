package day43_abstraction.language;

public interface Language {

    public abstract void hello(); // in interface we do not have to put public or abstract because they are public abstract by default

    void bye(); // it is public abstract by default
}
